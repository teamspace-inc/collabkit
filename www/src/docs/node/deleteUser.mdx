## Delete User

Use this function to delete an existing user.

```ts
import { deleteUser } from '@collabkit/node';

await deleteUser({
  appId: '<your app ID>',
  apiKey: '<your API key>',
  userId: '<user ID>',
});
```
