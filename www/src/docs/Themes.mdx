import { CollabKitProvider, Thread, ThemeProvider } from '@collabkit/react';
import { docDemoOverlay } from '../styles/docs/Docs.css';
import { DocLink } from './Doc';
import { ComponentDemo } from './ComponentDemo';
import { ThemeName } from './ThemeName';
import { renderCodeSnippet } from './CodeEditor';
import RenderAvatarExample from './RenderAvatarExample.tsx?raw';

# Themes

## Completely match your apps look and feel.

### What is a theme?

A theme is a set of styles that define the look and feel of CollabKit. It includes colors, fonts, and spacing. It can be used to match the look and feel of your app. Almost every single aspect of CollabKit can be customised.

### Built-in themes

CollabKit comes with two builtin themes. Light and Dark. Use these by passing light or dark to the `<CollabKitProvider />`.

<ComponentDemo hideThemeEditorButton={true}>
  <ThemeProvider theme="light">
    <ThemeName>Light Theme</ThemeName>
    <Thread
      style={{ width: 320, height: 320, borderRadius: 6 }}
      threadId={'thread1'}
      autoFocus={false}
    />
  </ThemeProvider>
</ComponentDemo>

<ComponentDemo hideThemeEditorButton={true}>
  <ThemeName>Dark Theme</ThemeName>
  <ThemeProvider theme="dark">
    <Thread
      style={{ width: 320, height: 320, borderRadius: 6 }}
      threadId={'thread1'}
      autoFocus={false}
    />
  </ThemeProvider>
</ComponentDemo>

### Writing your own custom theme

You can write your own custom theme by passing a `theme` object to the `<CollabKitProvider />`.

### Avatar customization

You can customize the avatar of the user by passing the `renderAvatar` prop to the `<CollabKitProvider />`.

For example, if you have an `Avatar` component in your app, you can pass it to the `renderAvatar` prop like this:

{renderCodeSnippet(RenderAvatarExample)}
