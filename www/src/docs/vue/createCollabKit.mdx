import { renderCodeSnippet } from '../CodeEditor';
import { createCollabKitUsage } from './code';

# createCollabKit

As an altenative to `CollabKitVueProvider`, CollabKit can also
be set up with a plugin in a Vue app.

If you need to pass in dynamic options, it's recommended To use `CollabKitVueProvider` instead.

## Usage

In `main.ts`:
{renderCodeSnippet(createCollabKitUsage)}
