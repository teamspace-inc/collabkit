{
  "private": true,
  "name": "collabkit-monorepo",
  "workspaces": [
    "examples/*",
    "packages/@collabkit/*",
    "org/*"
  ],
  "scripts": {
    "dev": "pnpm --recursive --parallel run dev",
    "server": "yarn workspace server dev"
  },
  "devDependencies": {
    "pnpm": "^7.5.0",
    "prettier": "^2.6.2"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 100
  }
}
